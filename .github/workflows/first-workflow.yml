name: first-w-action
on: workflow_dispatch
jobs:
  firstjob:
    runs-on: ubuntu-latest
    steps:
      - name: print welcome message 
        run: echo "Welcome to github actions:
      - name: performing actions
        run: echo "printing the current working directory"; ls
